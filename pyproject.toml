[project]
name = "price-watch"
version = "0.6.0"
requires-python = ">=3.13"

[tool.pytest.ini_options]
addopts = "--cov=core --cov=infrastructure --cov-branch --cov-report=term-missing --cov-fail-under=100"
testpaths = ["tests"]
